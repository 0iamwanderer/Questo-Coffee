rules_version = '2';

// Faz 0 — KİLİTLİ. Ürün görselleri yüklemesi Faz 5'te admin route handler üzerinden açılacak.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
